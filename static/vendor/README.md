mermaid 11.4.1
source: https://cdn.jsdelivr.net/npm/mermaid@11.4.1/dist/mermaid.min.js
licence: MIT

Vendored, not fetched at runtime: the presentation window has to render
diagrams with the network unplugged. Refresh by re-running the URL above and
checking the tail still sets globalThis.mermaid.
